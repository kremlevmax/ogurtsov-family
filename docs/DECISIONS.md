# Решения по неясностям

Формат: дата, решение, обоснование. Новые записи добавляются сверху.
Правило по умолчанию (CLAUDE.md, раздел 24): при новой неопределённости
выбираем самое простое решение, совместимое с `CLAUDE.md`.

## 2026-08-15 — Развёртывание

**`git push` по HTTPS через `credential.helper=osxkeychain` зависал без
вывода и без видимого системного диалога** — на этой машине push в
GitHub через штатный macOS keychain-хелпер полностью зависал (и через
Claude Code, и напрямую в терминале пользователя), без текста ошибки
даже при `GIT_TERMINAL_PROMPT=0`. Причина не установлена (возможно,
скрытый диалог авторизации keychain или конфликт с уже сохранёнными
некорректными credentials для github.com). Обошли, не разбираясь до
конца: `brew install gh` → `gh auth login --web` (браузерный
device-code flow, надёжно работает в неинтерактивном терминале) → `gh
auth setup-git`, который прописывает
`credential.https://github.com.helper = !gh auth git-credential`
поверх системного `osxkeychain` — после этого push прошёл сразу. Если
столкнётесь с тем же на другой машине — тот же обходной путь быстрее,
чем диагностировать keychain.

**Прод на Vercel использует тот же Supabase-проект, что и
разработка** — отдельный prod-проект Supabase не создавался: для
личного некоммерческого сайта такого масштаба разделение
dev/prod-баз добавляет сложность (два набора миграций, два места
создавать редакторов) без реальной выгоды — сайт и так изначально
публичный, скрывать в проде нечего от разработки, кроме самих
данных, которые как раз должны быть общими.

## 2026-08-15 — Этап 6

**Скрипты бэкапа используют `SUPABASE_SERVICE_ROLE_KEY`, если он
задан** — обычный anon-ключ проходит через RLS, а значит не видит
мягко удалённых людей/связей и служебные таблицы (`editors`,
`pending_uploads`). Для настоящего полного бэкапа это неприемлемо:
цель бэкапа — восстановить всё, включая то, что скрыто от публичного
чтения. `SUPABASE_SERVICE_ROLE_KEY` в `.env.local` уже был
зарезервирован под это именно с Этапа 0 (см. комментарий в
`lib/supabase/server.ts`). Ключ используется только в
`scripts/*.ts`, никогда не импортируется в код приложения. Если ключ
не задан, скрипт всё равно работает по anon-ключу, но печатает явное
предупреждение о неполноте бэкапа — не блокирует пользователя без
service-role key, но и не скрывает от него ограничение.

**`scripts/lib/r2.ts` дублирует часть `lib/r2/client.ts`/`objects.ts`,
а не переиспользует их напрямую** — те файлы начинаются с `import
"server-only"`, который **безусловно** бросает исключение при обычном
запуске через Node (пакет полагается на `exports`-условие
`react-server`, которое расставляет только бандлер Next.js; plain
`tsx` его не задаёт). Оборачивать/патчить это было бы более хрупким,
чем 20 строк независимого клиента — тот же принцип изоляции, что уже
применялся к `workers/media` (отдельный `tsconfig.json`).

**GEDCOM: HUSB/WIFE при неизвестном поле — произвольный, но
детерминированный fallback** — модель данных намеренно не хранит пол
человека (роль на связи «родитель→ребёнок» — `mother`/`father`/
`parent`, не атрибут персоны, CLAUDE.md 7.4). GEDCOM же требует
разложить пару по тегам `HUSB`/`WIFE`. Известные роли (`father`→HUSB,
`mother`→WIFE) выставляются первыми и никогда не перезаписываются;
только человек с ролью `parent` или без роли (бездетная
partner/spouse-связь) занимает оставшийся свободный слот. Это может
дать «неправильный» пол в стороннем ПО при импорте, но не ломает связи
между людьми — обмен родственными связями важнее точности пола,
которого у нас просто нет в данных.

**`SUPABASE_SERVICE_ROLE_KEY` не задаётся в Vercel на проде** —
приложению (в отличие от скриптов бэкапа) он не нужен вообще: все
операции чтения/записи идут через RLS с anon-ключом, авторизация
проверяется через `editors` и Server Actions (CLAUDE.md 13). Задавать
его в проде — это лишняя площадь атаки без выигрыша. См.
`docs/DEPLOYMENT.md`.

## 2026-08-15 — Этап 5

**Поиск остаётся клиентским, решение с Этапа 0 пересмотрено** — на
Этапе 0 было записано, что подключение к индексированному запросу
Supabase запланировано на Этап 5. По факту: главная страница и так
целиком загружает всех `people` на клиент, чтобы отрисовать дерево —
без этого canvas нечего показывать. Значит, поиск по уже загруженному
массиву даёт ноль обращений к БД на нажатие клавиши, что лучше, чем
дебounced-запрос по индексу на каждый символ (сетевая задержка на
каждое нажатие, дополнительный код без выигрыша). CLAUDE.md 8 требует
именно избежать full scan «при каждом символе» — это условие уже
выполнено строже, чем через ILIKE/trigram-запрос. DB-индексы
(`gin_trgm_ops` на `first_name/last_name/maiden_name/display_name` из
`0001_init.sql`, `middle_name` добавлен в `0003`) оставлены как задел
на случай, если дерево вырастет настолько, что придётся отказаться от
загрузки всех `people` разом — тогда поиск и рендер дерева нужно будет
пересматривать вместе, не по отдельности.

**`--color-fg-muted` затемнён с `#8a7a61` до `#726048`** — не проходил
WCAG AA (4.5:1) для обычного текста на `--color-bg`/`--color-bg-elevated`
(было 3.45:1/3.86:1), а токен используется именно для мелкого
вторичного текста (подписи, годы жизни), не только для крупного, где
допустим порог 3:1. Новое значение — 5.0:1/5.6:1, тот же тёплый
оттенок. Подробный расчёт — `docs/DESIGN_SYSTEM.md`.

**Офлайн-баннер глобальный, не по форме** — единственное недостающее
обязательное UI-состояние из CLAUDE.md 10. Сделан один
`components/layout/offline-banner.tsx` в корневом layout вместо
дублирования проверки `navigator.onLine` в каждой форме — ошибки
конкретных Server Actions (сеть работает, но запрос не прошёл)
по-прежнему показываются рядом с формой, как и раньше; баннер отвечает
только за случай «сети нет вообще».

## 2026-08-14 — Этап 4

**Миниатюры фотографий не генерируются** — CLAUDE.md 14 рекомендует
генерировать производные изображения при загрузке, чтобы не грузить
оригинал для превью. Для личного семейного архива (ожидаемо десятки,
не тысячи фото) это не создаёт заметной проблемы: используется
оригинал с `loading="lazy"` и ограничением размера в CSS. Добавить
полноценные миниатюры (например, через canvas на клиенте перед
загрузкой, вторым объектом в R2) можно отдельной задачей, если объём
данных вырастет — тогда потребуется новая колонка вроде
`thumbnail_object_key` в `media`, что осознанно отложено, чтобы не
делать миграцию под гипотетическую нагрузку.

**Magic-bytes проверяются не для всех форматов** — CLAUDE.md 3.8 просит
сверять фактический тип файла «когда это практически возможно».
Проверка идёт через ranged GET из R2 при finalize (без загрузки всего
файла), но сигнатуры прописаны только для распространённых и наиболее
подделываемых форматов (изображения, PDF, ZIP-контейнеры вроде
docx/xlsx/pptx/odt, mp3) — `lib/validation/media.ts#MAGIC_BYTES`. Для
остального (txt, csv, tar, часть аудиоформатов) полагаемся на
связку расширение+MIME; расширяемо при необходимости.

**Cloudflare Worker — отдельный деплой, не Next.js Route Handler** —
CLAUDE.md 5.4 явно требует отдельный минимальный Worker с R2-биндингом
для публичной отдачи, не проксирование через сервер приложения. Его
TypeScript-конфиг (`workers/media/tsconfig.json`,
`@cloudflare/workers-types`) изолирован от основного `tsconfig.json`
(`workers` в `exclude`), чтобы Worker-специфичные глобальные типы
(`R2Bucket` и т. д.) не просачивались в проверку типов приложения.

**Лайтбокс фотографий и панель человека используют один и тот же
паттерн порталов** — `PhotoLightbox` в `media-section.tsx` рендерится
через `createPortal` в `<body>` ровно по той же причине, что и
`PersonDrawer` (Этап 3): `position: fixed` внутри контейнеров с
`overflow: hidden`/трансформациями ненадёжен в WebKit.

**Медиа для панели дерева грузятся заранее для всех людей одним
запросом** (`listAllMediaGroupedByPerson`) — так же, как уже
преднагружены `people`/`relationships` для клиентского выбора без
повторных запросов к серверу. Для личного архива это остаётся дешёвым;
при росте данных можно перейти на подгрузку медиа по клику через
Server Action.

## 2026-08-14 — исправление Safari (два шага)

**Шаг 1 — React Portal в `<body>`** (`components/tree/person-drawer.tsx`):
панель использовала `position: fixed` внутри контейнера дерева с
`overflow: hidden` — в WebKit/Safari это исторически ведёт себя не по
спецификации. Портал убрал зависимость от родительских контейнеров.
Этого оказалось недостаточно: пользователь прислал скриншот — панель
открылась, но заняла только часть экрана (не на всю ширину/высоту и не
пристыкована вправо), хотя портал уже был применён.

**Шаг 2 — позиционирование через инлайн-стили и `window.matchMedia`,
а не Tailwind-классы `inset-y-0`/`right-0`/`left-auto`**
(`lib/hooks/use-media-query.ts`). Вероятная причина: Tailwind v4
компилирует часть позиционирующих классов через CSS logical properties
(`inset-block`/`inset-inline`), а мы смешивали это с shorthand-классом
`inset-0`, — именно такие комбинации браузеры расходятся в трактовке.
Заменил на явные `top`/`right`/`bottom`/`left`/`width` через обычный
JS-объект стилей, выбираемый по результату `matchMedia("(min-width:
768px)")` — самому стабильно реализованному способу узнать ширину
экрана во всех браузерах. Через Chrome-автоматизацию подтвердил, что
поведение не сломалось; в реальном Safari сам проверить не могу
(автоматизация работает только с Chrome) — нужно, чтобы пользователь
проверил ещё раз.

**Клик по узлу дерева — через настоящий `<button>`, а не `div
role="button"`** — на всякий случай сделал заодно: нативная кнопка
гарантированно одинаково обрабатывает клик/тап/клавиатуру во всех
браузерах, в отличие от ARIA-имитации на div.

## 2026-08-14 — Этап 3

**URL синхронизируется через `window.history.pushState`, а не
`router.push`** — маршрут `/` (`app/(public)/page.tsx`) не читает
`searchParams` на сервере; выбор человека меняет URL напрямую через
History API в клиентском компоненте `FamilyTreeExplorer`. Это
осознанный выбор: если бы страница читала `searchParams`, любое
открытие панели вызывало бы повторный RSC-запрос и пересборку дерева
(со сбросом pan/zoom) — ровно то, что запрещает CLAUDE.md 3.6/11
(«дерево остаётся видимым»). `useSearchParams()` используется только
один раз при монтировании, чтобы восстановить состояние по прямой
ссылке.

**Выбор человека (клик по узлу и выбор из поиска) — один и тот же
механизм** — оба действия просто меняют один и тот же `selectedPersonId`
в `FamilyTreeExplorer`, что одновременно открывает панель, обновляет
URL и центрирует дерево (`FocusOnSelection` в `tree-canvas.tsx`).
Не стал делать отдельную логику «подсветки» для поиска — упрощает код
и гарантирует одинаковое поведение.

**`PersonDetailContent` — общий компонент для панели и страницы
`/people/[id]`** — вместо дублирования вёрстки карточки в двух местах.
Разница только в одном пропе (`onPersonSelect`): если он передан
(внутри панели), клик по родственнику переключает панель на месте;
если нет (на отдельной странице), это обычная ссылка `<Link>`.

**CSS container queries (`@container`) вместо `lg:` для сетки
основные-данные/биография** — внутри панели шириной 480px
viewport-брейкпоинт `lg` срабатывал бы по ширине экрана, а не панели,
сжимая колонки. Tailwind v4 поддерживает container queries нативно, без
дополнительных пакетов.

**Проп `children` переименован в `childPeople`** — ESLint
(`react/no-children-prop`) справедливо ругается на проп с именем
`children`, не являющийся настоящими JSX-children. Переименовал вместо
отключения правила.

## 2026-08-14 — Этап 2

**Явный `is_deceased` вместо вывода «жив/умер» из даты смерти** —
изначально «жив ли человек» вычислялось как «есть дата смерти с
известной точностью». Пользователь попросил отдельный чекбокс:
неточность даты и факт смерти — разные вещи («точно умер, но когда —
неизвестно» раньше нельзя было записать). Добавлена колонка
`is_deceased boolean not null default false` (`0002_add_is_deceased.sql`),
применена к боевой базе. Форма при снятии галочки «жив» очищает дату и
место смерти, чтобы не оставалось рассинхронизированных данных.

**Кнопка «Добавить родителя» убрана из быстрых действий на странице
человека** — пользователь посчитал её избыточной рядом с «мать»/«отец».
Роль `parent` в базе (для неизвестного пола/заглушек) осталась
доступна через общую форму связей на странице редактирования —
убрана только кнопка-ярлык, не сама возможность.

**Быстрые действия живут на странице человека, а не в канве дерева** —
CLAUDE.md 3.6 описывает их как часть дерева, но полноценная боковая
панель дерева ещё не реализована (запланирована на Этап 3). Разместил
кнопки «Добавить мать/отца/супруга-партнёра/ребёнка» и «Редактировать»
на `/people/[id]`, видимые только вошедшему редактору — переносить в
канву буду вместе с панелью на Этапе 3, а не создавать промежуточный
временный UI поверх React Flow.

**Место рождения/смерти — простой текст с find-or-create в `places`**
— соответствует CLAUDE.md 3.9/12 («место может быть простым текстом»).
Редактор вводит название; сервер ищет существующую запись `places` по
точному совпадению имени без учёта регистра или создаёт новую.
Координаты/карта пока не добавляются через форму — это можно сделать
позже, отредактировав запись `places` напрямую, без изменения схемы.

## 2026-08-14 — дизайн по референсам

**Тема «warm archive» принята как финальная**, не временная — на
основе четырёх скриншотов владельца (`docs/design-references/`).
Заменили нейтральную палитру и Geist Sans на Playfair
Display (заголовки) + Lora (текст) + Geist Mono (метки/эйбрау),
терракотовый акцент вместо блёклого коричневого.

**Поиск перенесён в шапку сайта** (`components/layout/header.tsx`
получил опциональный проп `search`) — на референсе строка поиска стоит
в общей верхней панели рядом с заголовком, а не отдельным блоком под
ней. Используется только на главной странице.

**CSP разрешает `'unsafe-eval'` только в `NODE_ENV=development`** —
обнаружено визуальной проверкой через браузер: React в dev-режиме
использует `eval()` для отладочных стектрейсов, из-за чего Next.js dev
overlay показывал ошибку. В production React `eval()` не использует
вовсе, поэтому боевая политика осталась строгой без `unsafe-eval`.

**Ширина карточки человека в дереве увеличена до 272px** (было 220px)
— полные русские ФИО (например «Огурцова Наталья Игоревна (Белова)»)
обрезались. Имя в карточке теперь может занимать до двух строк
(`line-clamp-2`) вместо обрезки многоточием.

## 2026-08-14 — Этап 1

**`/edit` — только заглушка с подтверждением входа, без CRUD** — Этап
1 по `CLAUDE.md` (раздел 22) требует только login/logout и guard'ы, а
не формы. `/edit` показывает вошедшего редактора и кнопку выхода —
этого достаточно, чтобы проверить, что защита маршрута действительно
работает, не забегая вперёд Этапа 2.

**Проверка членства в `editors` дублируется на сервере, а не только в
RLS** — `/edit/page.tsx` после проверки `auth.getUser()` дополнительно
делает `select` из `editors`. Причина: аутентифицированный в Supabase
Auth пользователь — не то же самое, что редактор с правом записи;
CLAUDE.md 13 требует проверку авторизации и в Server Action/странице,
и в RLS, не полагаясь на одно из двух.

**`proxy.ts` — дополнительный, не единственный слой защиты** —
редиректит неавторизованные запросы к `/edit/*` до рендера страницы
(меньше лишней работы), но сама страница всё равно независимо
проверяет сессию и `editors`. Не полагаемся на один механизм.

**Публичные страницы остаются на demo-данных до Этапа 2** — несмотря
на то, что Supabase-клиенты и схема уже готовы, дерево и страницы
людей на `/` и `/people/[id]` не переключены на реальные запросы.
Причина: без форм создания людей (Этап 2) реальная база будет пустой,
и переключение сейчас просто показало бы пустое дерево вместо
демонстрационного — хуже, а не лучше для тестирования интерфейса.

## 2026-08-14 — Этап 0

**Family-unit слой в дереве** — используем составную промежуточную
модель: чистая функция `buildFamilyGraph` строит граф людей и
family-unit узлов без layout, отдельная функция `layoutFamilyGraph`
вызывает ELK, `buildReactFlowGraph` объединяет их для React Flow.
Причина: раздельное тестирование доменной логики (граф) от layout
(асинхронный, зависит от ELK).

**Layout на клиенте, не на сервере** — `TreeCanvas` вычисляет позиции
через `useEffect` в браузере, с состояниями loading/error. Причина:
`elkjs` можно было бы гонять и на сервере (bundled-сборка — чистый JS
без воркеров), но клиентский расчёт проще сочетается с последующим
pan/zoom/interaction слоем React Flow и не блокирует SSR.

**Поиск на Этапе 0 — реальный, но локальный** — `SearchBox` фильтрует
демо-данные в браузере через `features/search/normalize.ts`, а не
статичная заглушка без логики. Причина: сама функция нормализации и
поиска — часть обязательного тестового покрытия (CLAUDE.md 16), и
дешевле показать её работающей на fixture, чем строить и потом
выбрасывать non-functional UI. Подключение к индексированному запросу
Supabase — Этап 5, как и указано в `CLAUDE.md`.

**Клик по узлу дерева ведёт на `/people/<id>`, не открывает боковую
панель** — на Этапе 0 полноценная синхронизация URL ↔ открытая панель
и мобильный sheet не реализованы, так как явно относятся к Этапу 3
(`CLAUDE.md`, раздел 22). Промежуточное поведение — обычная навигация
на индексируемую страницу человека, которая и так обязательна для
Этапа 0.

**Демо-семья включает разведённых супругов, placeholder-родителей и
брак между двоюродными** — сделано намеренно, чтобы граф-трансформация
и (в перспективе) ELK-layout были проверены на нетривиальной топологии
до появления реальных семейных данных. Данные полностью вымышленные
(CLAUDE.md, раздел 17: seed не должен содержать реальную семью).

**Партнёрский уникальный индекс не поддерживает повторный брак с тем
же человеком** — см. «Известное упрощение» в `docs/DATA_MODEL.md`.
Простейшее решение, совместимое с MVP; при необходимости снимается
отдельной миграцией.

**`server-only` как явная зависимость** — добавлен пакет `server-only`
и импортирован в `lib/supabase/server.ts`, чтобы случайный импорт
серверного Supabase-клиента в клиентский компонент падал на этапе
сборки, а не создавал скрытую уязвимость.
